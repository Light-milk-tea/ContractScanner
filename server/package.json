{
  "name": "contract-scanner-server",
  "version": "0.1.0",
  "private": true,
  "description": "ContractScanner service skeleton for upload, task status, and analysis result APIs",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "test:citation-guard": "tsx scripts/check-citation-guard.ts"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.21.2"
  },
  "devDependencies": {
    "@types/express": "^5.0.3",
    "@types/node": "^24.0.15",
    "tsx": "^4.20.3",
    "typescript": "^5.8.3"
  }
}
