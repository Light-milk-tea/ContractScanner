# -*- coding: utf-8 -*-
"""4.3 技术路线：以文字介绍为主，少堆代码；仅写仓库真实能力。"""
from __future__ import annotations

from docx.shared import RGBColor


def write_section_43(b):
    """b: Builder from build_proposal_docx.py"""
    from docx.shared import Pt

    def src(text: str):
        b.text(f'（出处：{text}）', size=Pt(9), color=RGBColor(0x66, 0x66, 0x66), space_after=2)

    b.h('4.3 技术路线', 2)
    b.body(
        '本节说明《法言白话》如何把「看不懂的合同」变成「能看懂的风险结论」。'
        '叙述以已实现能力为准：凡仓库未落地的能力，一律放在边界说明中，不写入实现描写。'
        '技术名词尽量用业务语言解释；需要核对细节时，在段末标注出处，便于对照源码与文档。'
    )

    # ---------- 4.3.1 ----------
    b.h('4.3.1 总体思路与设计原则', 3)
    b.body(
        '总体思路可以概括成一句话：手机端负责「拿得到合同、看得懂结果」；'
        '云端分析服务负责「读得完合同、说得清风险」；知识库负责「给得准依据、挡得住编造」。'
        '三者分工清晰：客户端不直连大模型，模型调用统一放在服务端，既便于保管密钥，'
        '也便于在知识库故障时自动降级，保证主链路仍能给出结构化解读。'
    )
    src('README.md')

    b.body('围绕这一思路，项目坚持四条原则：')
    b.body(
        '第一，可信优先。白话解释与风险判断可以借助大模型生成，但具体法条引用必须来自知识库中的法条摘录；'
        '没有检索到依据时，宁可不写法律条文，也不允许模型凭空编造条号。'
        '第二，先看懂再求全。产品首要交付是摘要、红黄绿分级、原文对照白话与修改建议，'
        '而不是做成完整的 OCR 产品或法律意见书系统。'
        '第三，全场景贴合签约场景。应用接续支持换设备继续看结果，桌面卡片方便回看最近摘要，'
        '隐私防窥降低敏感页被截录屏的风险。'
        '第四，边界清晰。电子签署、律师咨询、合同自动改写、多人协作、正式法律意见书导出等，'
        '首版明确不做，避免方案夸大。'
    )
    src('README.md；doc/交接文档.md')

    # ---------- 4.3.2 ----------
    b.h('4.3.2 系统体系架构', 3)
    b.body(
        '系统采用「端—分析服务—知识库」三层结构。'
        '第一层是 HarmonyOS 客户端，负责导入合同、展示分析进度、呈现结果总览与条款对照，并提供合同追问；'
        '同时接入应用接续、互动卡片与隐私防窥。'
        '第二层是 Node.js 分析服务，负责任务排队与状态推进，完成文本提取与清洗、条款提示切分、'
        '向知识库检索、调用大模型生成结构化结果，并对法条字段做引用校验。'
        '第三层是 Python 知识库服务，基于向量检索返回风险规则、样例标注与法条摘录，供分析服务注入提示词。'
    )
    src('README.md「技术架构」；doc/交接文档.md')

    b.picture('fig2_architecture.png', width_cm=13.0)
    b.caption('图2 系统体系架构')

    b.body(
        '三层之间的协作方式是异步任务制：客户端提交分析请求后，先拿到任务编号；'
        '服务端在后台推进「识别与清洗—知识检索—模型分析」；客户端轮询任务状态，成功后再拉取完整结果。'
        '这样即使用户合同较长、模型耗时较高，界面仍能持续反馈进度，而不是长时间无响应。'
    )

    b.picture('fig4_sequence.png', width_cm=12.8)
    b.caption('图4 合同分析任务协作关系')

    b.body(
        '需要特别说明的是：当前分析任务保存在服务进程的内存中，适合比赛演示与联调；'
        '进程重启后历史任务不会自动保留。这是现阶段实现方式，并不等同于已上线的生产数据库方案。'
    )
    src('doc/交接文档.md')

    # ---------- 4.3.3 ----------
    b.h('4.3.3 端到端业务数据流', 3)
    b.body(
        '一次完整分析，在业务上可以分成五个阶段，各阶段状态对用户可见：等待开始、识别整理中、智能分析中、成功、失败。'
        '端侧与服务侧使用同一套状态含义，避免「后端已经成功、前端还在转圈」的语义错位。'
    )

    b.body(
        '进入分析后，服务端首先尽量拿到合同正文。优先使用客户端直接提交的文本内容'
        '（例如用户导入的文本，或手机端对合同照片识文后上传的正文）；'
        '若暂时拿不到正文，则仅作为联调兜底使用模板文本。'
        '若用户尝试直接上传尚未支持直解析的 PDF/DOCX 二进制文件，系统会明确提示改为导出图片或 TXT，'
        '而不是假装已经读完文件。这一设计与「OCR 是轻量能力、核心是解读」的产品定位一致。'
    )
    src('服务端文本提取与分析控制器实现；README 关于 OCR 边界的说明')

    b.body(
        '正文就绪后，系统做两件「为模型减负」的事：一是清洗换行与多余空白，让文本更整齐；'
        '二是按段落切出有限条条款提示，帮助后续知识检索更聚焦。'
        '接着，若知识库开启，则先根据合同开头与条款提示检索相关风险知识与允许引用的法条列表；'
        '检索失败时不中断分析，而是带着「本次无可引用法条」的约束继续调用大模型。'
        '最后由大模型输出结构化结果：整体摘要、签署前确认清单、若干条重点条款的原文、白话、风险等级、原因与建议。'
        '若模型试图填写法条，系统会再做一次核对，只保留本轮知识库允许列表中的内容。'
    )
    src('分析流水线与风险引用校验实现')

    # ---------- 4.3.4 ----------
    b.h('4.3.4 技术框架选型说明', 3)
    b.body(
        '选型服务于三个目标：鸿蒙赛道可演示、主链路可闭环、现场可兜底。'
        '客户端采用 HarmonyOS 的 ArkTS/ArkUI 与 Stage 模型，覆盖手机、平板与二合一设备形态，'
        '以便讲述全场景续看与卡片回看。'
        '分析服务采用 Express 与 TypeScript，便于快速定义任务接口、状态查询与结果协议。'
        '知识库服务采用 FastAPI 与 Chroma，把风险规则、样例标注、法条摘录做成可检索语料。'
        '大模型侧对接阿里云百炼的 OpenAI 兼容接口，默认使用可配置的通义模型，温度设置偏低，以换取更稳定的结构化输出。'
    )
    src('README 技术表；server 环境配置示例')

    b.table([
        ['层级', '主要技术', '在本项目中的作用'],
        ['客户端', 'HarmonyOS / ArkTS / ArkUI', '导入、进度、分层阅读、鸿蒙特性'],
        ['分析服务', 'Express / TypeScript', '任务编排、清洗切分、模型调用、引用校验'],
        ['知识库', 'FastAPI / Chroma / Python', '风险与法条检索'],
        ['大模型', '百炼兼容接口', '摘要、风险、白话、建议、追问回答'],
        ['端侧存储', 'Preferences', '最近记录、设置、接续与卡片上下文'],
        ['任务存储', '内存仓储', '演示期保存任务状态与结果'],
    ])
    b.text('', space_after=2)

    b.body(
        '模块关系上，客户端展示层、鸿蒙能力层、网络协议层、分析流水线、知识库与模型层各司其职。'
        '下图用于帮助理解「谁负责对用户说话、谁负责调用模型、谁负责提供依据」，'
        '不意味着存在额外未实现的中间件。'
    )
    b.picture('fig5_modules.png', width_cm=12.8)
    b.caption('图5 模块职责分解')

    # ---------- 4.3.5 ----------
    b.h('4.3.5 客户端技术路线：如何让用户「用得顺」', 3)
    b.body(
        '客户端技术路线围绕主链路展开：从首页进入导入，提交后进入分析中页面；'
        '成功后先看结果总览，再进入条款对照精读，必要时打开合同问答继续追问。'
        '设置页用于配置服务地址、密钥与隐私开关，并提供答辩用的异常演示模式。'
        '另有文档扫描页，支持对合同照片做端侧识文，再把识别出的正文交给分析服务。'
    )

    b.body(
        '交互上坚持「分层阅读」。先给整体结论和签署前确认项，再用红、黄、绿帮助用户抓住重点，'
        '然后才进入单条条款的原文与白话对照；追问放在最后，避免一上来就被长文淹没。'
        '这一信息架构直接对应结果总览、条款对照、合同问答三个页面，而不是停留在概念层。'
    )
    b.picture('fig1_interaction_flow.png', width_cm=13.0)
    b.caption('图1 主交互流程')
    b.picture('fig3_info_architecture.png', width_cm=13.0)
    b.caption('图3 结果阅读信息架构')

    b.body(
        '为了让等待过程可感知，分析页会根据任务状态更新文案，并以小步进度反馈减少「空白等待」。'
        '导入失败、分析失败、结果加载失败时，页面提供统一的空态/错误态与重试入口，'
        '用户可以明确知道下一步该重试、改服务地址，还是切换到本地演示样例。'
        '最近分析记录会缓存在本地，首页可回看，也作为桌面卡片的数据来源之一。'
    )
    src('客户端页面、AnalyzeVM、EmptyState/LoadingState、CacheService')

    b.body(
        '联调方面，模拟器与真机对「本机地址」的理解不同。项目会按运行环境解析默认服务地址，'
        '并支持在设置页改为电脑局域网 IP；同时提供健康检查探测，降低「服务其实没起来却一直转圈」的排查成本。'
        '关于文件格式：文本与图片识文路径是主推荐路径；对尚不支持直接解析的二进制文档，会明确提示转换后再导入。'
    )
    src('服务地址解析与导入服务实现')

    # ---------- 4.3.6 ----------
    b.h('4.3.6 分析服务技术路线：如何把合同变成结构化结论', 3)
    b.body(
        '分析服务对外提供简洁协议：创建分析任务、查询任务状态、获取分析结果，以及基于本次结果的追问。'
        '创建成功后立即返回任务编号，真正耗时的识别、检索与模型调用在后台进行。'
        '只有任务成功时才返回完整结果；失败时返回可读的错误原因，便于客户端展示与重试。'
    )

    b.body(
        '结构化结果是整条技术路线的「合同」。它至少包含：合同名称、整体摘要、签署前确认清单、'
        '红黄绿统计，以及若干条款条目。每条条款包含标题、原文摘录、白话解释、风险等级、风险原因，'
        '并可附修改建议；法条依据仅在有知识库支撑时出现。'
        '客户端把该结构映射为界面卡片与对照阅读块，保证「服务端说什么，页面就展示什么」，减少二次加工带来的偏差。'
    )
    src('doc/交接文档.md 数据契约；服务端响应结构')

    b.body(
        '知识注入策略强调「先检索、再生成、再校验」。'
        '检索时会结合合同开头与条款提示，尽量覆盖自动续约、押金、违约金等高频风险主题；'
        '注入给模型的内容分成两块：一块是可参考的风险知识，一块是「允许引用的法律条例」清单。'
        '模型被明确要求：原文必须来自本合同，法条只能从允许清单中选择，不能把风险规则标题伪装成法条。'
        '模型返回后，系统再次清洗法条字段，未匹配允许清单的内容会被去掉。'
        '这样形成「提示词约束 + 结果校验」的双重保险。'
    )
    src('大模型提示约束与 CitationGuard 实现；doc/交接文档.md')

    b.body(
        '在提示词设计上，系统角色被定义为中文合同审查助手，要求只输出结构化内容，不要额外说明文字；'
        '风险等级只能是红、黄、绿三档；签署前确认项需要给出可执行的检查建议。'
        '同时要求输出重点条款数量适中，避免一次抛出过多条目造成阅读负担。'
        '若模型偶发在结果前后附加解释，服务端会尽量提取其中完整的结构化对象，提升联调稳定性。'
    )
    src('服务端大模型分析模块')

    # ---------- 4.3.7 ----------
    b.h('4.3.7 合同追问技术路线：如何在结果之上继续澄清', 3)
    b.body(
        '一次性分析解决「先看懂」，追问解决「看懂之后还有疑惑」。'
        '追问不是开放域闲聊，而是绑定「本次已经完成的合同分析结果」：'
        '回答必须依据本合同原文、本次结构化结论，以及本轮检索到的知识；'
        '不确定时要明确说明不确定，并提醒用户结合原文或咨询专业人士。'
        '每次回答末尾固定附带免责声明，强调内容仅为解读辅助，不构成正式法律意见。'
    )
    src('合同追问模块实现')

    b.body(
        '为控制成本与跑偏风险，对话历史只保留有限轮次，合同原文过长时会截断后送入模型；'
        '回答可以附带相关条款编号，方便用户跳回对照页继续精读，但面向用户的正文中不暴露内部技术字段。'
        '客户端聊天页负责组织提问与展示回答，服务端负责在已成功任务上下文中生成答复。'
    )

    # ---------- 4.3.8 ----------
    b.h('4.3.8 知识库技术路线：如何让依据可追溯', 3)
    b.body(
        '知识库的目标不是「什么都往里塞」，而是服务两类用途：'
        '一是提示模型常见风险模式与样例经验，二是提供可引用的法条短摘录。'
        '因此语料分成风险规则、样例标注、法条摘录三类。'
        '其中只有法条摘录可以作为结果里的法律依据来源；风险规则可以指导风险识别，但不能冒充法条。'
    )
    src('agent/README.md')

    b.body(
        '以租房场景为例，风险规则会描述「超长自动续约」为何高风险、常见坑点是什么、更稳妥的改法方向是什么；'
        '样例标注则对应具体样例合同中的句子，说明它为什么被标红；'
        '法条摘录则提供如格式条款提示说明义务等短文本，供模型在相关时引用。'
        '当前仓库中，风险规则、样例标注、人工精选法条与批量导入法条均已建库可检索；'
        '无模型密钥时，知识库仍可用本地向量方式完成离线建库联调，保证开发与答辩准备不被密钥问题阻断。'
    )
    src('知识库语料与 agent/README.md')

    b.body(
        '知识库服务对外提供健康检查、检索与重建索引能力。'
        '分析服务通过检索接口获取命中结果，再整理成「知识说明」和「允许引用清单」两段文字交给大模型。'
        '若知识库关闭或超时，分析不会因此失败，而是降级为纯提示词分析，并禁止输出具体法条。'
        '这一降级策略是现场稳定性的关键保障。'
    )
    src('README；分析服务 RAG 降级逻辑')

    # ---------- 4.3.9 ----------
    b.h('4.3.9 鸿蒙全场景能力路线：如何让「看合同」发生在多设备生活里', 3)
    b.body(
        '赛题鼓励融合鸿蒙特性。本项目不堆砌无关能力，而是选择与签约阅读强相关的三项：'
        '应用接续、互动卡片、隐私防窥。'
    )

    b.body(
        '应用接续解决的是「手机上分析完，想换到平板慢慢读」。'
        '当用户在结果页停留时，系统会保存当前查看的任务上下文；'
        '在同账号其他设备上接续后，可以恢复到同一份结果继续阅读。'
        '代码链路已打通；跨设备实机效果建议在答辩前用同账号设备彩排一次。'
    )
    src('接续服务与 EntryAbility；doc/交接文档.md')

    b.body(
        '互动卡片解决的是「不用打开应用，也能回看最近一份合同摘要」。'
        '桌面卡片展示最近合同的关键信息，点击后可回到对应结果页。'
        '若暂无分析记录，卡片会显示空态提示，避免空白误导。'
    )
    src('卡片服务与桌面卡片配置')

    b.body(
        '隐私防窥解决的是「地铁、教室里看合同怕被偷看」。'
        '用户可在设置中开关该能力；开启后，进入结果总览与条款对照等敏感页时启用隐私窗口，'
        '限制截屏与录屏，离开页面后再恢复。'
        '它保护的是关键阅读页面，而不是宣称整机级安全产品。'
    )
    src('隐私防窥服务与结果/对照页调用')

    # ---------- 4.3.10 ----------
    b.h('4.3.10 稳定性与演示兜底路线', 3)
    b.body(
        '比赛与真实使用都可能遇到网络波动、服务未启动、模型超时等情况。'
        '因此技术路线把「失败可见、路径可替」当作一等需求。'
        '服务端侧：知识库不可用时降级继续分析；文本获取不到位时给出明确错误或联调提示。'
        '客户端侧：提供高风险与低风险本地样例，以及弱网、超时、服务失败、降级展示等答辩演示模式，'
        '可以在不依赖现场网络运气的前提下讲清主链路与异常反馈。'
    )
    src('Mock 与设置页演示模式；doc/交接文档.md 现场预案')

    b.body(
        '此外，分析结果可导出为便于手机打开的 Word 兼容报告，内容包含摘要、风险统计与条款明细，'
        '并再次声明不构成正式法律意见。导出是「带走备忘」，不是「自动生成法律文书」。'
    )
    src('报告导出实现')

    # ---------- 4.3.11 ----------
    b.h('4.3.11 运行与协作方式', 3)
    b.body(
        '日常运行顺序是：先启动知识库服务并完成索引，再启动分析服务并配置模型密钥，'
        '最后用 DevEco Studio 运行鸿蒙客户端。'
        '健康检查用于确认分析服务已就绪；真机联调时把服务地址改成电脑局域网地址。'
        '密钥只放在本地环境或设置页，不进入公开仓库。'
    )
    src('README 快速开始；agent/README.md')

    b.body(
        '仓库按职责分目录：客户端、分析服务、知识库与文档样例各成一块，'
        '便于分工协作，也便于评委按层核对。'
        '客户端侧重体验与鸿蒙能力，服务端侧重协议与模型约束，知识库侧重语料与检索，'
        '文档目录沉淀赛题说明、交接要点与测试样例合同。'
    )

    # ---------- 4.3.12 ----------
    b.h('4.3.12 边界说明：我们明确不声称什么', 3)
    b.body(
        '为避免「方案写得很满、代码对不上」，本节集中说明当前版本的边界。'
        '这些边界来自 README、交接文档与源码现实，不是额外营销话术。'
    )
    b.body(
        '当前没有生产级数据库、消息队列与对象存储，任务状态以内存方式保存，适合演示与开发联调。'
        '服务端没有做成商用 OCR/PDF 解析引擎；推荐路径是文本导入或图片识文后上传正文，'
        '对不支持直解析的文档会明确失败提示。'
        '仓库中存在个别占位或未接线文件，不以它们作为已交付能力来宣传。'
        '产品不提供电子签署、在线律师咨询、合同自动改写、多人协作编辑，也不输出正式法律意见书。'
        '客户端不会绕过服务端直接调用大模型。'
        '除已落地的应用接续、互动卡片、隐私防窥外，本说明不声称集成了其他未实现的鸿蒙特性。'
    )
    src('README「首版边界」；doc/交接文档.md；源码现状')

    b.h('4.3.13 技术路线小结', 3)
    b.body(
        '《法言白话》的技术路线，本质是一条可验证的「读懂合同」闭环：'
        '用鸿蒙客户端完成导入与分层阅读，用分析服务完成清洗、检索增强与结构化生成，'
        '用知识库与引用校验保证法条可追溯，用接续、卡片与防窥贴合真实阅读场景，'
        '再用降级与演示模式保证答辩与联调稳定。'
        '它追求的不是堆砌技术名词，而是让非法律用户在签字前，先看懂重点、看见风险、知道下一步该确认什么。'
    )


def estimate_note():
    return 'section_43 prose'
