# 《法言白话》项目实现步骤

> 依据 [项目完工工程架构蓝图](file:///Users/mac/DevEcoStudioProjects/ContractScanner/doc/项目完工工程架构蓝图.md) 第 10 节拆解。  
> 本文件既是执行清单，也是进度回写文件。后续 AI 每完成一步，必须自动修改本文件并标注状态。

---

## 1. 使用规则（给后续 AI）

### 1.1 执行规则
- 每次只执行一个最小步骤，不得跨步骤提前实现后续内容。
- 执行前先阅读与当前步骤相关的现有代码和配置。
- 若当前步骤依赖前一步未完成，先停止并说明阻塞原因，不得跳步。
- 若发现实现需要调整，以当前仓库代码为准，但不得偏离蓝图中的主链路目标。

### 1.2 回写规则
- 开始执行某一步前，将该步骤状态从 `待完成` 改为 `进行中`。
- 步骤完成后，必须将状态改为 `已完成`。
- 步骤完成后，必须同步补充：
  - 完成日期
  - 改动文件
  - 验证结果
  - 备注（可选）
- 如果执行失败或被阻塞：
  - 将状态改为 `阻塞`
  - 在“备注”中写明阻塞原因
  - 不得伪造“已完成”

### 1.3 状态约定
- `待完成`：尚未开始
- `进行中`：当前正在执行
- `已完成`：代码已落地，并完成基本验证
- `阻塞`：存在前置条件缺失或环境问题，当前无法继续

### 1.4 回写示例

```md
- [x] A1 验证基础工程可运行
  - 状态：已完成
  - 完成日期：2026-07-06
  - 改动文件：无
  - 验证结果：工程入口文件可正常加载 `pages/Index`
  - 备注：确认当前项目为 Empty Ability 初始模板
```

---

## 2. 当前执行原则

- 先做壳工程和主链路，再做鸿蒙特性，再做答辩强化。
- 首版优先保证“导入合同 -> 分析中 -> 结果总览 -> 对照阅读”完整跑通。
- 首版允许先用 Mock 数据和占位实现前后端联调，但 Mock 仅用于演示闭环，不代表核心能力已完成。
- 《法言白话》的核心用户价值依赖真实大模型能力，白话解释、风险原因、修改建议必须通过服务端接入大模型实现，不能以纯规则或纯 Mock 作为最终交付。
- 客户端只负责文件导入、状态轮询和结果展示，不直接调用第三方大模型 API；模型调用统一收敛在服务端。
- 不提前做二期能力：电子签署、律师咨询、合同改写、多人协作、法律意见书导出。

---

## 3. 阶段 A：工程起步

- [ ] A1 验证基础工程可运行
  - 状态：待完成
  - 目标：确认当前 Empty Ability 模板工程可正常打开、编译、加载首页。
  - 重点文件：`entry/src/main/ets/pages/Index.ets`、`entry/src/main/ets/entryability/EntryAbility.ets`、`entry/src/main/resources/base/profile/main_pages.json`
  - 完成标准：明确当前入口页、页面注册方式、Ability 入口方式。
  - 完成日期：
  - 改动文件：
  - 验证结果：
  - 备注：

- [x] A2 创建客户端目录骨架
  - 状态：已完成
  - 目标：建立 `app/pages/components/services/model/common` 基础目录。
  - 重点文件：`entry/src/main/ets/`
  - 完成标准：目录结构与蓝图一致，工程可识别，无非法路径。
  - 完成日期：2026-07-06
  - 改动文件：`entry/src/main/ets/services/api`、`entry/src/main/ets/services/ability`、`entry/src/main/ets/services/storage`、`entry/src/main/ets/services/parser`、`entry/src/main/ets/model/domain`、`entry/src/main/ets/model/enums`、`entry/src/main/ets/model/viewmodel`
  - 验证结果：已通过目录扫描确认 `entry/src/main/ets/` 下存在 `app/pages/components/services/model/common` 基础目录，且 `services`、`model` 已补齐蓝图要求的一级子目录；当前 shell 环境未安装 `hvigor`，未执行编译验证。
  - 备注：本步骤仅建立目录骨架，未移动现有入口文件，也未创建页面/组件/类型代码文件。

- [x] A3 创建空白页面文件骨架
  - 状态：已完成
  - 目标：创建 `HomePage`、`ImportPage`、`AnalyzingPage`、`ResultOverviewPage`、`ClauseComparePage`、`SettingsPage` 空页面。
  - 重点文件：`entry/src/main/ets/pages/*.ets`
  - 完成标准：页面文件存在，至少可编译通过。
  - 完成日期：2026-07-06
  - 改动文件：`entry/src/main/ets/pages/HomePage.ets`、`entry/src/main/ets/pages/ImportPage.ets`、`entry/src/main/ets/pages/AnalyzingPage.ets`、`entry/src/main/ets/pages/ResultOverviewPage.ets`、`entry/src/main/ets/pages/ClauseComparePage.ets`、`entry/src/main/ets/pages/SettingsPage.ets`
  - 验证结果：已确认 6 个页面骨架文件存在，命名与蓝图一致；文件采用统一的 `@Component export struct` 空实现，具备后续被入口页或路由接入的基础。当前 shell 环境未安装 `hvigor`，未执行编译验证。
  - 备注：保留现有 `Index.ets` 作为当前入口页，未在本步骤切换首页或注册路由。

- [x] A4 创建空白组件文件骨架
  - 状态：已完成
  - 目标：创建 `RiskBadge`、`SummaryCard`、`ClauseBlock`、`AnalyzeProgress`、`EmptyState`。
  - 重点文件：`entry/src/main/ets/components/*.ets`
  - 完成标准：组件文件存在，导出方式统一，可被后续页面引用。
  - 完成日期：2026-07-06
  - 改动文件：`entry/src/main/ets/components/RiskBadge.ets`、`entry/src/main/ets/components/SummaryCard.ets`、`entry/src/main/ets/components/ClauseBlock.ets`、`entry/src/main/ets/components/AnalyzeProgress.ets`、`entry/src/main/ets/components/EmptyState.ets`
  - 验证结果：已确认 5 个组件骨架文件存在，命名与蓝图一致；文件统一采用 `@Component export struct` 导出方式，后续页面可直接 import 引用。当前 shell 环境未安装 `hvigor`，未执行编译验证。
  - 备注：本步骤只创建组件空骨架，未增加业务属性、交互逻辑或样式主题依赖。

- [x] A5 创建公共模块
  - 状态：已完成
  - 目标：创建 `constants.ets`、`errors.ets`、`logger.ets`。
  - 重点文件：`entry/src/main/ets/common/*.ets`
  - 完成标准：页面路由、缓存 key、错误码、日志入口有统一定义。
  - 完成日期：2026-07-06
  - 改动文件：`entry/src/main/ets/common/constants.ets`、`entry/src/main/ets/common/errors.ets`、`entry/src/main/ets/common/logger.ets`
  - 验证结果：已创建公共常量、错误码与错误类型、日志入口封装；已通过诊断扫描确认无语法错误。当前 shell 环境未安装 `hvigor`，未执行编译验证。
  - 备注：路由封装将在 A7 实现；本步骤仅提供 route 常量键与 storage key 约定。

- [x] A6 创建基础枚举与领域模型
  - 状态：已完成
  - 目标：创建 `RiskLevel`、`AnalyzeStatus`、`ClauseRisk`、`AnalysisResult`、`Contract`。
  - 重点文件：`entry/src/main/ets/model/enums/*.ets`、`entry/src/main/ets/model/domain/*.ets`
  - 完成标准：第一版数据契约与蓝图一致，可供页面和接口共用。
  - 完成日期：2026-07-06
  - 改动文件：`entry/src/main/ets/model/enums/RiskLevel.ets`、`entry/src/main/ets/model/enums/AnalyzeStatus.ets`、`entry/src/main/ets/model/domain/ClauseRisk.ets`、`entry/src/main/ets/model/domain/AnalysisResult.ets`、`entry/src/main/ets/model/domain/Contract.ets`
  - 验证结果：已补齐基础枚举与领域模型，字段与蓝图数据契约对齐；已通过诊断扫描确认无语法错误。当前 shell 环境未安装 `hvigor`，未执行编译验证。
  - 备注：后续 DTO 映射与 API 返回结构以 `AnalysisResult` 为主，页面状态以 `AnalyzeStatus` 为主。

- [x] A7 创建路由封装
  - 状态：已完成
  - 目标：新增 `AppRouter.ets`，统一封装页面跳转。
  - 重点文件：`entry/src/main/ets/app/AppRouter.ets`
  - 完成标准：后续页面跳转不直接散落在各页面中。
  - 完成日期：2026-07-06
  - 改动文件：`entry/src/main/ets/app/AppRouter.ets`
  - 验证结果：已提供 `push/replace/back` 以及 `goHome/goImport/goAnalyzing/goResultOverview/goClauseCompare/goSettings` 统一入口；已通过诊断扫描确认无语法错误。当前 shell 环境未安装 `hvigor`，未执行编译验证。
  - 备注：页面 url 统一采用 `pages/*.ets` 的路径约定，具体页面注册见 A9。

- [x] A8 首页替换为业务首页
  - 状态：已完成
  - 目标：用 `HomePage.ets` 替换默认 Hello World 首页。
  - 重点文件：`entry/src/main/ets/pages/HomePage.ets`、`entry/src/main/ets/entryability/EntryAbility.ets`
  - 完成标准：首页显示项目标题、导入入口、最近记录占位区域。
  - 完成日期：2026-07-06
  - 改动文件：`entry/src/main/ets/pages/HomePage.ets`、`entry/src/main/ets/entryability/EntryAbility.ets`
  - 验证结果：`EntryAbility` 已切换加载 `pages/HomePage`；`HomePage` 已补齐标题、导入入口按钮、最近记录占位区域并标注 `@Entry`；已通过诊断扫描确认无语法错误。当前 shell 环境未安装 `hvigor`，未执行编译验证。
  - 备注：点击“导入合同”跳转将在 A10 实现。

- [x] A9 注册页面路由清单
  - 状态：已完成
  - 目标：更新页面配置，让新增页面被工程注册。
  - 重点文件：`entry/src/main/resources/base/profile/main_pages.json`
  - 完成标准：所有主页面已加入页面清单。
  - 完成日期：2026-07-06
  - 改动文件：`entry/src/main/resources/base/profile/main_pages.json`
  - 验证结果：已将 `HomePage/ImportPage/AnalyzingPage/ResultOverviewPage/ClauseComparePage/SettingsPage` 加入页面清单；已通过诊断扫描确认无语法错误。当前 shell 环境未安装 `hvigor`，未执行编译验证。
  - 备注：保留 `Index` 作为模板页面，后续可视情况移除。

- [x] A10 打通首页到导入页跳转
  - 状态：已完成
  - 目标：实现点击“导入合同”进入 `ImportPage`。
  - 重点文件：`HomePage.ets`、`ImportPage.ets`、`AppRouter.ets`
  - 完成标准：主链路第一跳可正常演示。
  - 完成日期：2026-07-06
  - 改动文件：`entry/src/main/ets/pages/HomePage.ets`、`entry/src/main/ets/pages/ImportPage.ets`
  - 验证结果：已将首页“导入合同”按钮接入 `goImport()`，并补齐导入页业务占位内容；已在华为模拟器中验证点击按钮后可正常进入 `ImportPage`，主链路第一跳可演示。
  - 备注：CLI 编译链路未在当前 shell 环境执行，由用户在 DevEco / hvigor 构建与模拟器联调中完成验证。

- [x] A11 创建 ViewModel 骨架
  - 状态：已完成
  - 目标：创建 `HomeVM`、`ImportVM`、`AnalyzeVM`、`ResultVM` 空实现。
  - 重点文件：`entry/src/main/ets/model/viewmodel/*.ets`
  - 完成标准：为后续页面状态收敛准备好位置。
  - 完成日期：2026-07-06
  - 改动文件：`entry/src/main/ets/model/viewmodel/HomeVM.ets`、`entry/src/main/ets/model/viewmodel/ImportVM.ets`、`entry/src/main/ets/model/viewmodel/AnalyzeVM.ets`、`entry/src/main/ets/model/viewmodel/ResultVM.ets`
  - 验证结果：已创建 4 个 ViewModel 骨架文件，并与现有领域模型/枚举建立最小依赖，可作为后续页面状态与接口数据收敛入口。
  - 备注：当前仅保留最小状态与更新方法，后续随页面与服务接入逐步扩展。

- [x] A12 阶段 A 验收
  - 状态：已完成
  - 目标：确认壳工程、目录骨架、基础数据契约全部落地。
  - 重点文件：阶段 A 全量改动文件
  - 完成标准：工程首页正常显示，导入页可进入，基础模型齐备。
  - 完成日期：2026-07-06
  - 改动文件：阶段 A 全量改动文件
  - 验证结果：已完成目录骨架、页面/组件骨架、公共模块、基础枚举与领域模型、路由封装、首页替换、页面注册、首页到导入页首跳、ViewModel 骨架；用户已确认 hvigor 构建无报错，且在华为模拟器中完成首页到导入页跳转验证，阶段 A 目标已验收通过。
  - 备注：后续继续以 DevEco 本地构建和模拟器联调为主推进阶段 B。

---

## 4. 阶段 B：主链路闭环

- [x] B1 创建网络请求基础封装
  - 状态：已完成
  - 目标：新增 `HttpClient.ets`，统一请求方法与错误处理入口。
  - 重点文件：`entry/src/main/ets/services/api/HttpClient.ets`
  - 完成标准：后续 API 调用不直接写裸请求代码。
  - 完成日期：2026-07-06
  - 改动文件：`entry/src/main/ets/services/api/HttpClient.ets`、`entry/src/main/module.json5`
  - 验证结果：已新增 `HttpClient.ets`，封装统一的 `GET/POST/request` 入口、基础请求头合并、状态码检查、JSON 字符串响应读取、`AppError` 异常映射与日志输出；同时已补充 `ohos.permission.INTERNET` 网络权限声明。当前 shell 环境未安装 `hvigor`，未执行编译验证，已完成代码级静态核对。
  - 备注：后续若接入文件上传进度或大文件流式传输，可在现有封装上继续扩展 multipart / stream 能力。

- [x] B2 创建合同分析接口服务
  - 状态：已完成
  - 目标：新增 `ContractApi.ets`，定义上传、查状态、取结果三个接口方法。
  - 重点文件：`entry/src/main/ets/services/api/ContractApi.ets`
  - 完成标准：接口方法签名与蓝图协议一致。
  - 完成日期：2026-07-06
  - 改动文件：`entry/src/main/ets/services/api/ContractApi.ets`
  - 验证结果：已新增 `ContractApi.ets`，定义 `uploadContract`、`getTaskStatus`、`getAnalysisResult` 三个接口方法，并补齐上传响应、任务状态、分析结果等 DTO 接口；接口路径已与蓝图中的 `/v1/contracts/analyze`、`/v1/tasks/{taskId}/status`、`/v1/tasks/{taskId}/result` 对齐，且补充了状态值合法性与 JSON 解析异常校验。当前 shell 环境未安装 `hvigor`，未执行编译验证，已完成代码级静态核对。
  - 备注：上传方法当前先以客户端文件元信息作为请求入参承接接口契约，实际文件选择与上传体组装将在 B4 文件导入服务阶段继续衔接。

- [x] B3 创建 DTO 映射层
  - 状态：已完成
  - 目标：新增 `DTOMapper.ets`，把服务端返回映射成客户端领域模型。
  - 重点文件：`entry/src/main/ets/services/api/DTOMapper.ets`
  - 完成标准：接口数据与页面展示数据分层清晰。
  - 完成日期：2026-07-06
  - 改动文件：`entry/src/main/ets/services/api/DTOMapper.ets`
  - 验证结果：已新增 `DTOMapper.ets`，提供 `mapAnalysisResult`、`mapClauseRisk`、`mapRiskLevel` 等映射方法，将 API DTO 映射为领域模型（含 `RiskLevel` 严格枚举转换与基础缺省容错）。当前 shell 环境未安装 `hvigor`，未执行编译验证，已完成代码级静态核对。
  - 备注：后续可将 DTO 定义从 `ContractApi.ets` 拆分到独立 `dto/` 目录，避免 API 文件膨胀。

- [x] B4 创建文件导入服务
  - 状态：已完成
  - 目标：新增 `FileImportService.ets`，统一处理文件类型识别和导入预处理。
  - 重点文件：`entry/src/main/ets/services/parser/FileImportService.ets`
  - 完成标准：导入逻辑不直接写在页面里。
  - 完成日期：2026-07-06
  - 改动文件：`entry/src/main/ets/services/parser/FileImportService.ets`
  - 验证结果：已新增 `FileImportService.ets`，提供 `parseFile`（文件名解析、扩展名提取、类型推断与支持校验）与 `buildUploadRequest`（生成上传接口入参）两类能力，导入预处理逻辑从页面剥离；不支持的格式统一抛出 `IMPORT_001`。当前 shell 环境未安装 `hvigor`，未执行编译验证，已完成代码级静态核对。
  - 备注：实际文件选择（Picker）与文件上传体（multipart/stream）将在后续页面交互与服务端联调阶段继续补齐。

- [x] B5 实现导入页基础交互
  - 状态：已完成
  - 目标：导入页支持选择合同来源、展示文件名、触发开始分析。
  - 重点文件：`ImportPage.ets`、`ImportVM.ets`
  - 完成标准：用户可完成“选择文件 -> 点击分析”的操作。
  - 完成日期：2026-07-06
  - 改动文件：`entry/src/main/ets/pages/ImportPage.ets`、`entry/src/main/ets/model/viewmodel/ImportVM.ets`、`entry/src/main/ets/common/constants.ets`、`entry/src/main/ets/app/AppRouter.ets`
  - 验证结果：导入页已支持选择导入方式（拍照/相册/本地文件）、手动填写文件 URI/文件名并展示已选择文件信息；点击“开始分析”会调用 `FileImportService` 进行文件类型识别与校验，随后触发 `ContractApi.uploadContract` 创建任务并携带 `taskId` 跳转分析页。当前 shell 环境未安装 `hvigor`，未执行编译验证，已完成代码级静态核对。
  - 备注：文件选择器（相册/本地文档 Picker）与真正的文件上传体组装将在后续联调阶段继续补齐。

- [x] B6 实现分析中页面
  - 状态：已完成
  - 目标：分析页展示状态文本、进度感知、重试入口。
  - 重点文件：`AnalyzingPage.ets`、`AnalyzeProgress.ets`、`AnalyzeVM.ets`
  - 完成标准：至少覆盖 `PENDING`、`OCR_RUNNING`、`LLM_RUNNING`、`FAILED`。
  - 完成日期：2026-07-06
  - 改动文件：`entry/src/main/ets/pages/AnalyzingPage.ets`、`entry/src/main/ets/components/AnalyzeProgress.ets`、`entry/src/main/ets/app/AppRouter.ets`
  - 验证结果：分析页已支持接收路由参数 `taskId` 并展示任务信息；提供状态文案与线性进度展示，覆盖 `PENDING`、`OCR_RUNNING`、`LLM_RUNNING`、`FAILED`、`SUCCESS`；失败状态提供“重试”入口，成功状态提供“查看结果”入口。当前 shell 环境未安装 `hvigor`，未执行编译验证，已完成代码级静态核对。
  - 备注：当前轮询为轻量实现，用于支撑页面状态演示；后续可在 B10 统一抽象轮询策略（间隔、超时、重试次数）并与服务端协议联调。

- [ ] B7 实现结果总览页
  - 状态：待完成
  - 目标：展示摘要、风险统计、风险条目列表。
  - 重点文件：`ResultOverviewPage.ets`、`SummaryCard.ets`、`RiskBadge.ets`、`ResultVM.ets`
  - 完成标准：成功结果页结构完整，可用于演示。
  - 完成日期：
  - 改动文件：
  - 验证结果：
  - 备注：

- [ ] B8 实现对照阅读页
  - 状态：待完成
  - 目标：展示原文、白话解释、风险原因、建议。
  - 重点文件：`ClauseComparePage.ets`、`ClauseBlock.ets`
  - 完成标准：点击风险项可查看对应条款详情。
  - 完成日期：
  - 改动文件：
  - 验证结果：
  - 备注：

- [ ] B9 打通页面主链路
  - 状态：待完成
  - 目标：实现 `Home -> Import -> Analyzing -> ResultOverview -> ClauseCompare`。
  - 重点文件：相关页面、`AppRouter.ets`
  - 完成标准：主链路页面跳转完整，无断点。
  - 完成日期：
  - 改动文件：
  - 验证结果：
  - 备注：

- [ ] B10 接入任务状态轮询
  - 状态：待完成
  - 目标：分析页接入状态轮询，按状态切换文案和页面行为。
  - 重点文件：`ContractApi.ets`、`AnalyzeVM.ets`、`AnalyzingPage.ets`
  - 完成标准：可从分析中自动流转到成功或失败。
  - 完成日期：
  - 改动文件：
  - 验证结果：
  - 备注：

- [ ] B11 接入结果拉取与展示
  - 状态：待完成
  - 目标：状态成功后自动拉取 `AnalysisResult` 并进入结果页。
  - 重点文件：`ContractApi.ets`、`DTOMapper.ets`、`ResultVM.ets`
  - 完成标准：结果页渲染的数据来自统一领域模型。
  - 完成日期：
  - 改动文件：
  - 验证结果：
  - 备注：

- [ ] B12 实现失败提示与重试
  - 状态：待完成
  - 目标：覆盖导入失败、轮询失败、分析失败、结果失败场景。
  - 重点文件：`ImportPage.ets`、`AnalyzingPage.ets`、`EmptyState.ets`、`errors.ets`
  - 完成标准：异常路径对用户可见，并提供明确重试入口。
  - 完成日期：
  - 改动文件：
  - 验证结果：
  - 备注：

- [ ] B13 创建本地缓存服务
  - 状态：待完成
  - 目标：新增 `CacheService.ets` 与 `SessionStore.ets`，缓存最近 5 条记录。
  - 重点文件：`entry/src/main/ets/services/storage/*.ets`
  - 完成标准：首页可显示最近分析记录。
  - 完成日期：
  - 改动文件：
  - 验证结果：
  - 备注：

- [ ] B14 首页接入最近记录
  - 状态：待完成
  - 目标：首页展示最近记录列表，并支持点击进入结果页。
  - 重点文件：`HomePage.ets`、`HomeVM.ets`、`SessionStore.ets`
  - 完成标准：首页具备业务感，不再只是静态入口。
  - 完成日期：
  - 改动文件：
  - 验证结果：
  - 备注：

- [ ] B15 准备 Mock 数据闭环
  - 状态：待完成
  - 目标：在真实大模型服务未接入前，提供可演示的模拟数据与模拟状态流转。
  - 重点文件：`ContractApi.ets`、Mock 数据文件或常量定义、`server/src/mock/*`（如已建）
  - 完成标准：断网或模型服务缺席时，仍可完整演示主链路，但文档中明确标注为临时方案。
  - 完成日期：
  - 改动文件：
  - 验证结果：
  - 备注：本步骤只解决演示闭环，不可作为“AI 能力完成”的验收依据。

- [ ] B16 创建服务端分析骨架
  - 状态：待完成
  - 目标：新增 `server/` 目录与分析服务基础结构，提供上传、查状态、取结果三个服务端接口入口。
  - 重点文件：`server/package.json`、`server/src/api/routes/*`、`server/src/api/controllers/*`、`server/src/config/*`
  - 完成标准：服务端工程可启动，三个基础接口具备可联调骨架。
  - 完成日期：
  - 改动文件：
  - 验证结果：
  - 备注：

- [ ] B17 接入真实大模型服务
  - 状态：待完成
  - 目标：完成服务端大模型 Provider 接入、API Key 配置、提示词模板与结构化输出约束。
  - 重点文件：`server/src/core/llm/*`、`server/src/config/*`、`server/.env*`（如新增）
  - 完成标准：服务端可基于真实大模型返回摘要、风险等级、白话解释、风险原因、建议等结构化结果。
  - 完成日期：
  - 改动文件：
  - 验证结果：
  - 备注：优先保证中文合同理解、结构化 JSON 输出和现场稳定性，不要求首轮即做到最低成本最优。

- [ ] B18 完成真实分析链路联调
  - 状态：待完成
  - 目标：打通客户端、服务端、真实大模型三段链路，完成从上传到结果展示的联调。
  - 重点文件：`ContractApi.ets`、`DTOMapper.ets`、`AnalyzeVM.ets`、`ResultVM.ets`、`server/src/core/llm/*`
  - 完成标准：客户端展示的数据来自真实服务端分析结果，非纯 Mock 返回。
  - 完成日期：
  - 改动文件：
  - 验证结果：
  - 备注：若因额度、网络或配额限制无法稳定调用，需保留 Mock 降级开关，但默认演示链路应优先走真实模型。

- [ ] B19 阶段 B 验收
  - 状态：待完成
  - 目标：确认主链路闭环、异常路径和真实大模型分析链路都可演示。
  - 重点文件：阶段 B 全量改动文件
  - 完成标准：主链路走通，失败重试可见，最近记录可回看，且真实大模型可稳定产出白话解释与风险结论。
  - 完成日期：
  - 改动文件：
  - 验证结果：
  - 备注：

---

## 5. 阶段 C：鸿蒙特性集成

- [ ] C1 实现设置页骨架
  - 状态：待完成
  - 目标：新增设置页，放置隐私开关、日志开关等控制项。
  - 重点文件：`SettingsPage.ets`
  - 完成标准：设置页可进入，可展示开关项。
  - 完成日期：
  - 改动文件：
  - 验证结果：
  - 备注：

- [ ] C2 接入隐私防窥服务
  - 状态：待完成
  - 目标：创建 `PrivacyGuardService.ets`，完成开关状态管理和系统能力接入。
  - 重点文件：`PrivacyGuardService.ets`、`SettingsPage.ets`
  - 完成标准：用户可在设置页开启或关闭隐私防窥。
  - 完成日期：
  - 改动文件：
  - 验证结果：
  - 备注：

- [ ] C3 接入应用接续服务
  - 状态：待完成
  - 目标：创建 `ContinueService.ets`，封装跨设备续读所需数据。
  - 重点文件：`ContinueService.ets`、`EntryAbility.ets`
  - 完成标准：至少具备“从结果页恢复查看”的接续数据结构和接入点。
  - 完成日期：
  - 改动文件：
  - 验证结果：
  - 备注：

- [ ] C4 完成接续场景联调
  - 状态：待完成
  - 目标：打通手机到平板或 PC 的结果页续读演示。
  - 重点文件：`ContinueService.ets`、相关路由恢复逻辑
  - 完成标准：接续演示链路清晰，失败时有兜底逻辑。
  - 完成日期：
  - 改动文件：
  - 验证结果：
  - 备注：

- [ ] C5 接入互动卡片服务
  - 状态：待完成
  - 目标：创建 `CardService.ets`，定义最近合同摘要卡片和点击跳转参数。
  - 重点文件：`CardService.ets`
  - 完成标准：卡片信息结构与结果页路由参数统一。
  - 完成日期：
  - 改动文件：
  - 验证结果：
  - 备注：

- [ ] C6 打通卡片深链跳转
  - 状态：待完成
  - 目标：点击卡片后进入指定任务的结果总览页。
  - 重点文件：`CardService.ets`、`EntryAbility.ets`、`AppRouter.ets`
  - 完成标准：卡片跳转路径可现场演示。
  - 完成日期：
  - 改动文件：
  - 验证结果：
  - 备注：

- [ ] C7 阶段 C 验收
  - 状态：待完成
  - 目标：确认应用接续、互动卡片、隐私防窥三项特性可见可讲。
  - 重点文件：阶段 C 全量改动文件
  - 完成标准：三项能力至少具备可演示版本。
  - 完成日期：
  - 改动文件：
  - 验证结果：
  - 备注：

---

## 6. 阶段 D：答辩强化与稳定性

- [ ] D1 统一空态与错误态组件
  - 状态：待完成
  - 目标：补齐 `EmptyState.ets` 并统一页面异常反馈样式。
  - 重点文件：`EmptyState.ets`、相关页面
  - 完成标准：空态、错误态、重试入口样式统一。
  - 完成日期：
  - 改动文件：
  - 验证结果：
  - 备注：

- [ ] D2 打磨加载与交互反馈
  - 状态：待完成
  - 目标：统一按钮态、分析中反馈、列表加载反馈。
  - 重点文件：相关页面与组件
  - 完成标准：主要页面无明显“空白等待”体验。
  - 完成日期：
  - 改动文件：
  - 验证结果：
  - 备注：

- [ ] D3 准备高风险与低风险样例数据
  - 状态：待完成
  - 目标：准备至少两份演示合同或等价 Mock 数据。
  - 重点文件：样例数据文件、Mock 配置
  - 完成标准：答辩中可快速展示两类不同风险结果。
  - 完成日期：
  - 改动文件：
  - 验证结果：
  - 备注：

- [ ] D4 实现弱网与超时演示开关
  - 状态：待完成
  - 目标：提供模拟超时、服务失败、降级展示的能力。
  - 重点文件：`ContractApi.ets`、调试配置或 Mock 控制项
  - 完成标准：可主动触发异常演示，不依赖现场网络偶发问题。
  - 完成日期：
  - 改动文件：
  - 验证结果：
  - 备注：

- [ ] D5 补充最小测试集
  - 状态：待完成
  - 目标：覆盖页面主链路、状态流转、缓存最近记录等关键点。
  - 重点文件：`entry/src/test/`、`entry/src/ohosTest/`
  - 完成标准：至少具备最小可运行测试集。
  - 完成日期：
  - 改动文件：
  - 验证结果：
  - 备注：

- [ ] D6 补齐日志与排障信息
  - 状态：待完成
  - 目标：统一关键日志点，便于答辩现场定位问题。
  - 重点文件：`logger.ets`、页面和服务调用处
  - 完成标准：上传、轮询、结果解析、路由跳转、异常均有日志。
  - 完成日期：
  - 改动文件：
  - 验证结果：
  - 备注：

- [ ] D7 输出答辩演示脚本
  - 状态：待完成
  - 目标：整理正常链路与异常链路的讲解脚本。
  - 重点文件：建议新增 `doc/演示脚本.md`
  - 完成标准：3 分钟内可顺畅讲清产品价值、AI 能力、鸿蒙亮点。
  - 完成日期：
  - 改动文件：
  - 验证结果：
  - 备注：

- [ ] D8 输出风险预案
  - 状态：待完成
  - 目标：整理后端异常、接续失效、卡片失效、弱网等现场预案。
  - 重点文件：建议新增 `doc/风险预案.md`
  - 完成标准：每类核心风险至少有一个降级说法或替代演示路径。
  - 完成日期：
  - 改动文件：
  - 验证结果：
  - 备注：

- [ ] D9 阶段 D 验收
  - 状态：待完成
  - 目标：确认项目达到“稳定答辩版本”。
  - 重点文件：阶段 D 全量改动文件
  - 完成标准：可完成正常演示、异常演示、鸿蒙特性演示三部分。
  - 完成日期：
  - 改动文件：
  - 验证结果：
  - 备注：

---

## 7. 总体验收门槛

- [ ] 主链路 100% 可走通：导入 -> 分析 -> 总览 -> 对照阅读
- [ ] 异常路径可恢复：导入失败、分析失败、超时、结果异常
- [ ] 三项鸿蒙特性可现场演示：应用接续、互动卡片、隐私防窥
- [ ] 首页支持查看最近 5 条分析记录
- [ ] 风险结果具备原文定位、白话解释、风险原因、修改建议
- [ ] 真实大模型已接入服务端主链路，默认演示不依赖纯 Mock 返回
- [ ] 至少准备两份样例数据：高风险、低风险
- [ ] 具备最小日志与测试支撑
- [ ] 具备答辩脚本和风险预案

---

## 8. 推荐执行顺序

`A1 -> A2 -> A3 -> A4 -> A5 -> A6 -> A7 -> A8 -> A9 -> A10 -> A11 -> A12 -> B1 -> B2 -> B3 -> B4 -> B5 -> B6 -> B7 -> B8 -> B9 -> B10 -> B11 -> B12 -> B13 -> B14 -> B15 -> B16 -> B17 -> B18 -> B19 -> C1 -> C2 -> C3 -> C4 -> C5 -> C6 -> C7 -> D1 -> D2 -> D3 -> D4 -> D5 -> D6 -> D7 -> D8 -> D9`

---

## 9. 后续 AI 的固定输出格式

每次执行完一个步骤后，除了正常回复用户，还必须同步更新本文件，并在回复中按以下格式汇报：

```md
已完成步骤：A1
改动文件：
- xxx
- xxx

验证结果：
- xxx

本文件状态已更新：
- A1：已完成
```
